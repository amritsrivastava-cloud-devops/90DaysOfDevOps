# 🐧 Linux Cheat Sheet – Senior / Principal Level

This document covers **core to advanced Linux commands** required for **L2, Senior, and Principal Linux Administrators**, DevOps Engineers, and SRE roles.

---

## 🔤 Wildcards & Globbing

| Symbol | Explanation |
|------|------------|
| `*` | Matches zero or more characters (e.g. `*.txt`) |
| `?` | Matches exactly one character (e.g. `Doc?.docx`) |

---

## 📂 Navigation & File Listing

| Command | Explanation |
|------|------------|
| `ls` | List files and directories |
| `ls -l` | Long listing with permissions and timestamps |
| `ls -a` | Include hidden files |
| `ls -t` | Sort by modification time |
| `pwd` | Show current working directory |
| `cd` | Go to home directory |
| `cd ..` | Go to parent directory |
| `cd /etc` | Change directory |

---

## 📄 File Comparison

| Command | Explanation |
|------|------------|
| `cmp A B` | Compare two files byte-by-byte |
| `diff A B` | Show line-by-line differences |

---

## 🗂 File & Directory Management

| Command | Explanation |
|------|------------|
| `mkdir X` | Create directory |
| `mkdir -p a/b/c` | Create nested directories |
| `mv A B` | Move or rename files |
| `cp A B` | Copy file |
| `cp -r A B` | Copy directory recursively |
| `rm X` | Remove file |
| `rm -r Y` | Remove directory recursively |
| `rm -rf Y` | Force delete directory |
| `rmdir Y` | Remove empty directory |

---

## 📝 File Viewing & Editing

| Command | Explanation |
|------|------------|
| `touch X` | Create empty file |
| `cat X` | View file content |
| `cat -b X` | Show line numbers |
| `wc X` | Word/line/byte count |
| `head -n 10 X` | First 10 lines |
| `tail -n 10 X` | Last 10 lines |
| `tail -f X` | Follow log file |
| `less X` | Scrollable file viewer |
| `ln -s A S` | Create symbolic link |

---

## 🔀 Input / Output Redirection

| Command | Explanation |
|------|------------|
| `cmd > file` | Redirect output (overwrite) |
| `cmd >> file` | Append output |
| `cmd < file` | Redirect input |
| `cmd 2> err` | Redirect stderr |
| `cmd &> file` | Redirect stdout + stderr |
| `cmd > /dev/null` | Suppress output |
| `cmd << EOF` | Here-document input |

---

## 🔍 Search & Text Processing

| Command | Explanation |
|------|------------|
| `grep patt file` | Search pattern |
| `grep -r patt dir` | Recursive search |
| `grep -i patt file` | Case-insensitive |
| `grep -v patt file` | Exclude matches |
| `find /path -name "*.sh"` | Find files |
| `locate filename` | Fast file search |
| `sort file` | Sort output |

---

## 📦 Archives & Compression

| Command | Explanation |
|------|------------|
| `tar -cf a.tar dir` | Create tar archive |
| `tar -xf a.tar` | Extract tar |
| `tar -czf a.tar.gz dir` | Gzip tar |
| `tar -xzf a.tar.gz` | Extract gzip |
| `gzip file` | Compress file |
| `gunzip file.gz` | Decompress |
| `zip -r a.zip dir` | Create zip |
| `unzip a.zip` | Extract zip |

---

## 🔐 File Permissions & Ownership

| Command | Explanation |
|------|------------|
| `chmod 755 file` | rwx for owner |
| `chmod +x file` | Make executable |
| `chown user file` | Change owner |
| `chgrp group file` | Change group |
| `chown user:group file` | Owner + group |
| `umask` | Default permission mask |

---

## 👤 User & Group Management

| Command | Explanation |
|------|------------|
| `who` | Logged-in users |
| `w` | User activity |
| `whoami` | Current user |
| `id` | UID and groups |
| `last` | Login history |
| `groupadd gp1` | Create group |
| `useradd -m user` | Create user |
| `userdel user` | Delete user |
| `usermod -aG grp user` | Add to group |

---

## ⚙️ Process Management & Performance

| Command | Explanation |
|------|------------|
| `ps aux` | Process list |
| `top` | Real-time monitoring |
| `htop` | Enhanced top |
| `atop` | Advanced monitoring |
| `kill PID` | Terminate process |
| `killall name` | Kill by name |
| `lsof` | Open files |
| `mpstat 1` | CPU stats |
| `vmstat 1` | Memory stats |
| `iostat 1` | Disk I/O stats |
| `watch df -h` | Periodic command |

---

## 💽 Disk & Memory

| Command | Explanation |
|------|------------|
| `df -h` | Disk usage |
| `du -sh dir` | Directory size |
| `fdisk -l` | Partition table |
| `free -h` | Memory usage |

---

## 🌐 Networking

| Command | Explanation |
|------|------------|
| `ifconfig` | Interface info (legacy) |
| `ip a` | Show IP addresses |
| `ethtool eth0` | NIC details |
| `netstat -tulpn` | Open ports |
| `ss -lntup` | Modern socket stats |
| `ping host` | Connectivity test |
| `traceroute host` | Path trace |
| `dig domain` | DNS lookup |
| `host domain` | DNS resolution |
| `wget URL` | Download |
| `curl URL` | Fetch content |
| `tcpdump -i eth0` | Packet capture |

---

## 🔥 Firewall & Security

| Command | Explanation |
|------|------------|
| `ufw allow 22` | Allow port |
| `ufw status` | Firewall status |
| `iptables -L` | Firewall rules |

---

## 📦 Package Management

### Debian / Ubuntu
| Command | Explanation |
|------|------------|
| `apt update` | Refresh repo |
| `apt upgrade` | Upgrade packages |
| `apt install pkg` | Install |
| `apt remove pkg` | Remove |

### RHEL / Rocky / Fedora
| Command | Explanation |
|------|------------|
| `dnf install pkg` | Install |
| `dnf update` | Update |

---

## 🧬 System Information & Hardware

| Command | Explanation |
|------|------------|
| `uname -a` | Kernel info |
| `uptime` | Load info |
| `hostnamectl` | Host identity |
| `dmesg` | Kernel logs |
| `lspci` | PCI devices |
| `lsusb` | USB devices |
| `dmidecode` | Hardware info |

---

## 🏆 Senior Interview Rule

> **Always think in layers:**  
> Hardware → Kernel → Network → Disk → Process → Service → Logs

This sheet is **production-grade** and **interview-defensible**.
